name: minimax_provider
title: MiniMax Provider
description: Adds MiniMax (M2.7) as an LLM provider option in Agent Zero. Cost-effective OpenAI-compatible model with AA Intelligence 50, $0.30/$1.20 per MTok.
version: 1.0.0
always_enabled: true
